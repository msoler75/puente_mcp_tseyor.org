{
  "name": "mcp-stdio-bridge",
  "version": "1.0.0",
  "description": "Puente entre Claude Desktop (stdio) y servidor MCP HTTP",
  "main": "mcp-bridge.js",
  "scripts": {
    "start": "node mcp-bridge.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.5.0"
  },
  "bin": {
    "mcp-bridge": "./mcp-bridge.js"
  }
}
